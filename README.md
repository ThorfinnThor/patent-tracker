# Patent Tracker

Two sector pages (Biotech/Pharma and Tech) built from PatentsView PatentSearch API data.

## Setup

1. Create a PatentsView API key and set:
   - PATENTSVIEW_API_KEY

2. Install Python deps:
```bash
pip install -r requirements.txt
# patent-tracker
